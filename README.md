## Notes

```sh
# List active users in a scratch org
$ sfdx force:user:list -u <alias>

# Generate user password
$ sfdx force:user:password:generate --targetusername <username>

# Get user info
$ sfdx force:user:display --targetusername <username>
```